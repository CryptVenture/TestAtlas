---
framework: cli
status: generated-but-not-validated
generated_by: scripts/generate-automation.js
---

# CLI test skeleton

This template is the language-agnostic shape of every CLI test emitted by
`scripts/generate-automation.js --cli`. The script writes a `.sh` file under
`_testatlas/tests/generated_automation/cli/`.

## Status lifecycle

`generated-but-not-validated` → `validated` → `committed` → `flaky`

Status lives in the companion `<slug>.meta.json`.

## File shape (POSIX sh)

```sh
#!/usr/bin/env bash
# _testatlas/tests/generated_automation/cli/<slug>.sh
# Generated by scripts/generate-automation.js — status: generated-but-not-validated.
# Source flow: FLOW-<id> (<flow name>).
#
# Fixtures required:
#   - (fixture files this script reads from)
# Mock data plan:
#   - (mocked binaries, env vars, sandbox paths)

set -eu

# Drive the documented CLI command(s).
# Documented expectation: <expected behavior, e.g. exit 0>
echo "TODO: replace with actual command" >&2
exit 0
```

## Required sections

1. Generated banner with status + source flow.
2. Fixtures + mock data comment block.
3. `set -eu` (or shellcheck-clean equivalent).
4. The actual CLI invocation(s) matching the documented entry point.
5. Companion `<slug>.meta.json` tracking status history.
