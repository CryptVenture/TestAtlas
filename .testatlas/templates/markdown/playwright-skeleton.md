---
framework: playwright
status: generated-but-not-validated
generated_by: scripts/generate-automation.js
---

# Playwright skeleton

This template is the language-agnostic shape of every Playwright file emitted
by `scripts/generate-automation.js --playwright`. The script writes a real
`.spec.ts` file under `_testatlas/tests/generated_automation/playwright/`; this
markdown reference exists so adapter authors and reviewers can audit the shape
without reading the generator.

## Status lifecycle

`generated-but-not-validated` → `validated` → `committed` → `flaky`

Status lives in the companion `<slug>.meta.json` next to each spec.

## File shape (TypeScript)

```ts
// _testatlas/tests/generated_automation/playwright/<slug>.spec.ts
// Generated by scripts/generate-automation.js — status: generated-but-not-validated.
// Source flow: FLOW-<id> (<flow name>).
//
// Fixtures required:
//   - (declare any fixture files this skeleton depends on)
// Mock data plan:
//   - (describe mocked services, response shapes, time anchors)

import { expect, test } from '@playwright/test';

test.describe('FLOW-<id> — <flow name>', () => {
  test('walks the documented happy path', async ({ page }) => {
    await page.goto('<entry-point>');
    // Drive documented steps; replace with real selectors and actions.
    await expect(page).toHaveURL(/.*/);
  });
});
```

## Required sections

1. Generated banner with status + source flow.
2. Fixtures + mock data comment block.
3. `test.describe` per flow id.
4. At least one `test()` walking the documented happy path.
5. Companion `<slug>.meta.json` tracking status history.
