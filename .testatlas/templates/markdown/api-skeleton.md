---
framework: api
status: generated-but-not-validated
generated_by: scripts/generate-automation.js
---

# API test skeleton

This template is the language-agnostic shape of every API test emitted by
`scripts/generate-automation.js --api`. The script writes a `.http` file under
`_testatlas/tests/generated_automation/api/` (compatible with VS Code REST
Client, JetBrains HTTP, Bruno-cli, hurl).

## Status lifecycle

`generated-but-not-validated` → `validated` → `committed` → `flaky`

Status lives in the companion `<slug>.meta.json` next to each .http file.

## File shape

```http
### _testatlas/tests/generated_automation/api/<slug>.http
### Generated by scripts/generate-automation.js — status: generated-but-not-validated.
### Source flow: FLOW-<id> (<flow name>).
###
### Fixtures required:
###   - (fixture files; e.g. _testatlas/fixtures/<flow>/seed.json)
### Mock data plan:
###   - (mocked services and their canned responses)

GET {{baseUrl}}/<entry-point>
Accept: application/json

### Expected: 200 OK
```

## Required sections

1. Generated banner with status + source flow.
2. Fixtures + mock data block (using `###` comments).
3. At least one HTTP request matching the documented entry point.
4. Companion `<slug>.meta.json` tracking status history.
