---
id: handoff-PLACEHOLDER
assignedRole: explorer
createdOn: 0000-00-00T00:00:00Z
createdBy: orchestrator
status: pending
---

# Sub-Agent Handoff: PLACEHOLDER

> Documents a focused task handed to a sub-agent. PRD §25. The handoff is the contract between the orchestrating agent and the worker.

## Objective

(One sentence: what should the sub-agent accomplish?)

## Scope

(Bulleted list: what files, domains, flows, surfaces are in scope?)

## Non-Scope

(Bulleted list: what must the sub-agent NOT touch?)

## Files To Read

(Files the sub-agent must read first to ground itself.)

## Files May Update

(Files the sub-agent may write to. Anything outside this list is read-only.)

## Required Evidence

(What evidence must the sub-agent capture and store under `evidence/`?)

## Questions

(Open questions the sub-agent should answer or escalate.)

## Constraints

(Time limits, safety constraints, env constraints, tooling constraints.)

## Output Location

(Where does the sub-agent write its results? Path under `sub_agents/` or back into the canonical files.)

## Output Structure

(Schema or template for the output. Reference a schema file or describe the markdown shape.)

## Completion Criteria

(Specific conditions that must hold before the sub-agent reports DONE.)
