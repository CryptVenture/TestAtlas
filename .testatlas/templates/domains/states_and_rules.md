# States and Rules

> Documents the domain's state machine, transition rules, permission gates, and visible indicators.

## Domain States

(Enumerate every meaningful state this domain can be in. Each state should have a label and a description.)

## Transition Rules

(For each pair of states, what triggers the transition? What conditions must be true?)

## Permissions

(Which roles can transition between which states? Which states are read-only for which roles?)

## Visible Indicators

(How is the current state communicated to the user? Badge, banner, color, icon, status pill?)
