# Data Contracts

> Documents the data shapes this domain produces and consumes.

## Entities

(List the first-class entities owned by this domain. Each entity should link to its `data/<slug>.md` record.)

## Fields

(For each entity, list the fields, their types, and their constraints.)

## Relationships

(How do entities relate to each other? One-to-many, many-to-many, polymorphic?)

## Validation Rules

(What server-side validation runs on writes? What client-side validation guards inputs?)
