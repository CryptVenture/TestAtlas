# Test Notes

> Documents how this domain is tested. Strategy, mapped tests, executed tests, coverage notes.

## Test Strategy

(Domain-specific testing strategy. What level of coverage is the goal? Which surfaces are tested first?)

## Tests Mapped

(List of test scenarios planned for this domain. Each entry links to `tests/<slug>.md`.)

## Tests Executed

(List of test runs that touched this domain. Each entry links to a `tests/run-<id>.md`.)

## Coverage Notes

(Narrative about coverage gaps, retest cadence, and confidence assessment.)
