# Dependencies

> Documents every system this domain depends on — APIs, components, entities, integrations, external services.

## APIs

(Internal APIs this domain calls. Link to per-endpoint records under `api/`.)

## Components

(Shared UI components this domain reuses. Link to per-component records under `components/`.)

## Entities

(Data entities this domain reads or writes. Link to per-entity records under `data/`.)

## Integrations

(Third-party integrations this domain talks to. Link to per-integration records under `integrations/`.)

## External Services

(External services not modeled as integrations: CDNs, DNS, analytics, observability.)
