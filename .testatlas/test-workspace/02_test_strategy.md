# 02 Test Strategy

Strategy: this workspace exists so CI can run `validate-workspace` against a
known-good `_testatlas/`-shaped tree on every PR. No real testing is performed
against this workspace.
