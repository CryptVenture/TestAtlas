// scripts/lib/update-core.js
//
// Plan 07-03 — atomic update orchestrator (UPDATE-02, UPDATE-06).
//
// Real implementation lands in Task 3; this Task 1 scaffold throws so any
// unexpected import path triggers a clear failure rather than silent no-op.

const NOT_YET = 'scripts/lib/update-core.js: not yet implemented (Task 3 lands the orchestrator)';

/**
 * Scaffold; Task 3 implements.
 * @param {object} _opts
 */
export async function runUpdate(_opts) {
  throw new Error(NOT_YET);
}
